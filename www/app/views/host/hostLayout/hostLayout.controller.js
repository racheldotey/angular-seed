'use strict';

/* 
 * Page Layout: Host
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the host (authenticated)
 * area of the app.
 */

angular.module('app.host.layout', [])
        .controller('HostLayoutCtrl', ['$scope', function ($scope) {
        
    }]);