'use strict';

/* 
 * Page Layout: Venue
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the venue (authenticated)
 * area of the app.
 */

angular.module('app.venue.layout', [])
        .controller('VenueLayoutCtrl', ['$scope', function ($scope) {
        
    }]);